import React from "react";

export default function Intersection({text, spanText}){
    return (
        <section className="intersection-container">
            <h1>{text}<span>{spanText}</span></h1>
        </section>
        
    
    )
}